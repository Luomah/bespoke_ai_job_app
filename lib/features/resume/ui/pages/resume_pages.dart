
import 'package:flutter/material.dart';

class ResumePages extends StatelessWidget {
  const ResumePages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("RESUME PAGEs"),);
  }
}